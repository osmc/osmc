all:
	@echo "Usage: make vero2\nmake vero3"

vero2:
	sudo bash build.sh vero2

vero3:
	sudo bash build.sh vero3

clean:
	sudo rm -f *.deb >/dev/null 2>&1
	sudo rm -rf files/usr/osmc/lib > /dev/null 2>&1
	sudo rm -rf files-dev/usr/osmc/include>/dev/null 2>&1
