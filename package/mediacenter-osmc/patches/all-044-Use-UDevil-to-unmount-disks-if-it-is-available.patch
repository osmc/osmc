From e1c89e78ad46d60bd8cff9667527b4d402f155c4 Mon Sep 17 00:00:00 2001
From: Sam Nazarko <email@samnazarko.co.uk>
Date: Tue, 13 Oct 2015 09:34:23 +0100
Subject: [PATCH] Use UDevil to unmount disks if it is available

Signed-off-by: Sam Nazarko <email@samnazarko.co.uk>
---
 xbmc/storage/linux/UDevProvider.cpp | 12 +++++++++++-
 1 file changed, 11 insertions(+), 1 deletion(-)

diff --git a/xbmc/storage/linux/UDevProvider.cpp b/xbmc/storage/linux/UDevProvider.cpp
index e3fcda0..5a1337d 100644
--- a/xbmc/storage/linux/UDevProvider.cpp
+++ b/xbmc/storage/linux/UDevProvider.cpp
@@ -214,7 +214,17 @@ bool CUDevProvider::Eject(const std::string& mountpath)
 {
   // just go ahead and try to umount the disk
   // if it does umount, life is good, if not, no loss.
-  std::string cmd = "umount \"" + mountpath + "\"";
+  std::string cmd;
+  if (access("/usr/bin/udevil", F_OK) != -1)
+  {
+      cmd = "/usr/bin/udevil umount \"" + mountpath + "\"";
+      CLog::Log(LOGDEBUG, "Going to use /bin/umount to perform an unmount operation");
+  }
+  else
+  {
+      cmd = "/bin/umount \"" + mountpath + "\"";
+      CLog::Log(LOGDEBUG, "Going to use /usr/bin/udevil to perform an unmount operation");
+  }
   int status = system(cmd.c_str());
 
   if (status == 0)
-- 
2.1.0

