VNC server for Raspberry Pi
<p>
It is possible to install a VNC server for Raspberry Pi. This is based on dispmanx and as such as a fairly limited frame rate, but it is still useful for those in headless installations.
</p>
<p>
You can install the VNC Server via My OSMC -> App Store -> VNC Server.
</p>
